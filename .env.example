# Briefly Docker Compose Configuration
# Copy this file to .env and fill in your values
# Usage: cp .env.example .env
#
# SECURITY WARNING:
# - NEVER commit the .env file to version control (it's in .gitignore)
# - NEVER share your .env file with others
# - Use 'chmod 600 .env' to restrict file permissions
# - Choose unpredictable ntfy topic names (treat them as sensitive)
# - For production, consider using Docker secrets (see docker-compose.secrets.yml)

# ===========================================
# LLM Provider Configuration
# ===========================================

# Which LLM provider to use: "claude" or "gemini"
BRIEFLY_LLM_PROVIDER=claude

# Optional: Specific model to use (leave empty for provider defaults)
# Claude examples: claude-3-7-sonnet-latest, claude-sonnet-4-5, claude-opus-4-5-20251101
# Gemini examples: gemini-2.5-flash, gemini-2.5-pro, gemini-2.0-flash
BRIEFLY_LLM_MODEL=

# API Keys (at least one required based on provider)
ANTHROPIC_API_KEY=your-anthropic-api-key-here
GOOGLE_API_KEY=your-google-api-key-here

# ===========================================
# Directory Configuration
# ===========================================

# Host directories to mount (relative or absolute paths)
# These directories will be created if they don't exist
BRIEFLY_HOST_INBOX=./inbox
BRIEFLY_HOST_OUTPUT=./output

# ===========================================
# Whisper Configuration
# ===========================================

# Whisper model size: tiny, base, small, medium, large
# Larger models are more accurate but slower and use more memory
# - tiny:   ~1GB RAM, fastest
# - base:   ~1GB RAM, good balance (default)
# - small:  ~2GB RAM, better accuracy
# - medium: ~5GB RAM, high accuracy
# - large:  ~10GB RAM, highest accuracy
BRIEFLY_WHISPER_MODEL=base

# ===========================================
# Notification Configuration
# ===========================================

# Optional: ntfy.sh topic for push notifications
# Leave empty to disable notifications
#
# SECURITY: Anyone who knows your topic name can subscribe to it!
# Use unpredictable topic names, e.g.: briefly-8f3k2n9x-notifications
# Avoid simple/guessable names like: my-notifications, john-briefly, etc.
#
# Example: briefly-a8f3k2n9x-notifications
BRIEFLY_NTFY_TOPIC=

# ===========================================
# Watchtower Configuration
# ===========================================

# How often to check for updates (in seconds)
# 3600 = 1 hour, 21600 = 6 hours, 86400 = 24 hours
WATCHTOWER_POLL_INTERVAL=86400

# Enable debug logging for Watchtower (true/false)
WATCHTOWER_DEBUG=false

# ===========================================
# Optional: Watchtower Notifications
# ===========================================

# Uncomment to enable Watchtower notifications via email
# WATCHTOWER_NOTIFICATIONS=shoutrrr
# WATCHTOWER_NOTIFICATION_URL=smtp://username:password@smtp.example.com:587/?from=watchtower@example.com&to=you@example.com

# Uncomment to enable Watchtower notifications via ntfy.sh
# WATCHTOWER_NOTIFICATIONS=shoutrrr
# WATCHTOWER_NOTIFICATION_URL=ntfy://ntfy.sh/your-watchtower-topic
